module.exports = (app) => {

    app.get('/', function (request, response) {
        response.send(
           
           <html>
            <body>
                 Ultima School Server
            </body> 
        </html>

        );
    })}; 